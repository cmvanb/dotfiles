[user]
	email = draakie@protonmail.com
	name = Casper
[core]
	editor = nvim
	pager = view
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
